# Language setting
language: node_js

# Version number
node_js:
  - "0.10"

build:
  pre_ci_boot:
    image_name: docker.link.fish:4888/linkfish/ubuntu-web-dev-shippable
    image_tag: latest
    pull: true

  ci:
    - ./showEnv.sh
    - echo $(echo $FOO)
    - echo $(echo $FIZZ)

  on_failure:
    - echo "The job failed!!!!!"

  on_success:
    - echo "The job succeeded!!!!"
