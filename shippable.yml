# Language setting
language: node_js

# Version number
node_js:
  - "0.10"
  
env:
  global:
    - FOO="bar|baz"
    - FIZZ=bin

build:
  pre_ci_boot:
    image_name: drydock/u14nod
    image_tag: prod
    pull: false

  ci:
    #- for i in `seq 1 10`; do echo "Waiting... $i"; sleep 1; done;
    - foobar
    - ssh-add -l

  on_failure:
    - echo "The job failed!!!!!"
    
  on_success:
    - echo "The job succeeded!!!!"
