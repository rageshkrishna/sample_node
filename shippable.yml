# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  - FOO=bar ABC=def
  - NO=build

build:
  ci:
    - echo "Hello, World!"

  on_failure:
    - echo "The job failed!!!!!"

  on_success:
    - echo "The job succeeded!!!!"

matrix:
  include:
    - node_js: "4.2.0"
      env: FIZZ=bin BAZ=bin
      gemfile: /src/gem.file
  allow_failures:
    - env: NO=build
