# Language setting
language: node_js

# Version number
node_js:
  - "0.12"

integrations:
  hub:
    - integrationName: "Ragesh - Docker"
      type: docker

#git:
#  submodules: false

build:
  pre_ci_boot:
    image_name: drydock/u14nodall
    image_tag: prod
    pull: false
    options: '-e fizz=bin'

  ci:
    - echo "Hello, World!"
    - ./loop.sh

  on_failure:
    - echo "The job failed!!!!!"

  on_success:
    - echo "The job succeeded!!!!"
