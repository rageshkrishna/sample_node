# Language setting
language: node_js

# Version number
node_js:
  - "0.12"
  
services:
  - postgres

integrations:
  hub:
    - integrationName: "Ragesh - Docker"
      type: docker

#git:
#  submodules: false

build:
  pre_ci_boot:
    image_name: ubuntu
    image_tag: 14.04
    pull: false
    options: '-e fizz=bin'

  ci:
    - whoami
    - echo $HOME
    - env
    - ssh-add -l
    - ssh -vT git@github.com
    - echo "Hello, World!"

  on_failure:
    - echo "The job failed!!!!!"
    
  on_success:
    - echo "The job succeeded!!!!"
