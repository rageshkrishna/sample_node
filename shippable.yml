# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  
env:
  - FOO=bar

build:
  ci:
    - echo "Hello, World!"

  on_failure:
    - echo "The job failed!!!!!"

  on_success:
    - echo "The job succeeded!!!!"

matrix:
  include:
    - node_js: "4.2.0"
      env: FIZZ=bin
      gemfile: /src/gem.file
  exclude:
    - node_js: 0.12
